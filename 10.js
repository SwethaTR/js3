//Use forEach to console.log each name in the names array.
const names=["Swetha","Harshi","Sam"]
const stname=arr=>{
    names.forEach(function(name){
        console.log(name)
    })
}
console.log(stname(names))